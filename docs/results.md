# Results

> **Rule for this file**: every number here is copied from a real benchmark
> run's artifacts (`artifacts/benchmarks/<run_id>/`) produced by
> `scripts/benchmark.py` on the disclosed hardware. Nothing is estimated.

*No benchmark run has been recorded yet for the current revision. This file
is populated by running:*

```bash
uv run python scripts/benchmark.py --config configs/benchmark/default.yaml
```

*and transcribing the run's `summary.md` / plots here together with the
hardware disclosure from its `records.jsonl`.*
